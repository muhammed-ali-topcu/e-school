setup:

academicYear:
Grades:
Sections:
  # columns: grade_id, Name, is_active
  init
  create section for each grade
  CRUD in panel
Subjects:
  # columns: grade_id, Name, is_active,description
  init:
    migration
    model
    seeder

Students:
  # columns: name,grande_id,section_id,is_active,number,birth_date,enrollment_date
  init:
    migration
    model
    seeder & factory
  crud:




teachers:
  init
  assign teacher to subjects

week program:



attends:


exams:

teacher auth:

settings:
translations:
